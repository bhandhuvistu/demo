apiVersion: v1
kind: Service
metadata:
  name: shopping-service
spec:
  type: LoadBalancer
  selector:
    app: shopping-app
  ports:
    - name: http
      port: 80        # External port
      targetPort: 8282   # Container port inside pod
